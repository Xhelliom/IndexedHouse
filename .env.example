# =============================
# IndexedHouse - Configuration
# =============================
# Ce fichier liste les variables d'environnement nécessaires pour démarrer
# la stack via docker-compose. Copie-le en `.env` pour un démarrage rapide.
# Astuce: adapte les secrets en prod.

# --- Web (Next.js) ---
# URL publique utilisée côté navigateur pour joindre l'API via Nginx
NEXT_PUBLIC_API_URL=http://localhost

# --- Base de données Postgres ---
# Identifiants et base créés par docker-compose
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=indexedhouse

# --- Stockage objet MinIO (S3 compatible) ---
# Identifiants d'admin pour accéder au console MinIO et aux API S3
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# --- Secrets applicatifs ---
# Secret JWT pour signer/valider les tokens
JWT_SECRET=dev-secret-change-me

# Clé API OpenAI pour les fonctionnalités de vision/embeddings
OPENAI_API_KEY=sk-your-openai-key

# Notes:
# - L'API récupère la configuration S3 (endpoint, region, bucket) via docker-compose.
# - Tu peux modifier les valeurs ci-dessus avant de démarrer pour plus de sécurité.
